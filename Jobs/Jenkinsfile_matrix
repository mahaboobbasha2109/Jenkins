matrix {
    axes {
        axis {
            name 'PLATFORM'
            values 'linux', 'mac', 'windows'
        }
    }
    stages {
        stage('build') {
            echo "build"
        }
        stage('test') {
            echo "test"
        }
        stage('deploy') {
            echo "deploy"
        }
    }
}